let trainingData=[];export function setTrainingData(t){trainingData=t}export function classifyState(t,n=3){if(0===trainingData.length||!t||0===t.length)return"UNKNOWN";const e=trainingData.map(n=>({label:n.label,distance:euclideanDistance(t,n.features)}));e.sort((t,n)=>t.distance-n.distance);const a=e.slice(0,Math.min(n,e.length)),i={};a.forEach(t=>{i[t.label]=(i[t.label]||0)+1});let r="UNKNOWN",l=0;for(const[t,n]of Object.entries(i))n>l&&(l=n,r=t);return r}function euclideanDistance(t,n){let e=0;const a=Math.min(t.length,n.length);for(let i=0;i<a;i++)e+=(t[i]-n[i])**2;return Math.sqrt(e)}export function parseTrainingCSV(t){const n=t.split("\n").filter(t=>t.trim()),e=[];for(let t=isNaN(parseFloat(n[0].split(",")[0]))?1:0;t<n.length;t++){const a=n[t].split(",").map(t=>t.trim());if(a.length<2)continue;const i=a.pop(),r=a.map(Number);r.some(isNaN)||e.push({features:r,label:i})}return e}